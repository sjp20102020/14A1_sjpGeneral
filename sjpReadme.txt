https://github.com/ngbp/ngbp


$ git clone git://github.com/joshdmiller/ng-boilerplate
$ cd ng-boilerplate
$ sudo npm -g install grunt-cli karma bower
$ npm install
$ bower install
$ grunt watch   //use /node_modules/.bin/grunt

$ grunt compile   //in bin folder
$ grunt //build and compile

